@model List<Library.DualGlobe.ERP.Models.SalaryDetail>

@{
    ViewBag.Title = "View Levy";
}

<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        View Levy
    </h1>
    <ol class="breadcrumb">
        <li><a href="@Url.Action("Index", "Home")"><i class="fa fa-dashboard"></i> Home</a></li>
        <li class="active">View Levy</li>
    </ol>
</section>
<!-- Main content -->
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-body">
                    <table id="tblClient" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                <th>Employee Id</th>
                                <th>Employee Name</th>
                                <th>Work Status</th>
                                <th>Levy Amount</th>
                            </tr>
                        </thead>

                        @if (Model != null)
                        {
                            <tbody>
                                @{
                                    foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@System.Configuration.ConfigurationManager.AppSettings["EmployeeIdPrefix"]@item.EmployeeId</td>
                                            <td>@item.EmployeeName</td>
                                            <td>@item.WorkStatus</td>
                                            <td style="text-align:right;">S$@item.Levy.GetValueOrDefault(0)</td>
                                        </tr>
                                    }
                                }
                            </tbody>
                         }
                    </table>

                </div>
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
    <!-- /.col -->
</section>
<!-- /.content -->